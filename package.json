{
  "name": "@ctrl/tracker-definitions",
  "version": "0.0.0-placeholder",
  "description": "API definitions for your favorite tracker",
  "author": "Scott Cooper <scttcper@gmail.com>",
  "license": "MIT",
  "repository": "typectrl/jackett-definitions",
  "keywords": [
    "Torznab",
    "jackett",
    "torrent",
    "tracker",
    "typescript"
  ],
  "source": "src/index.ts",
  "main": "./src/index.js",
  "typings": "./src/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "update": "ts-node -O '{\"module\": \"commonjs\"}' scripts/build-definitions.ts",
    "lint": "tslint -t stylish -p .",
    "lint:fix": "tslint -p tsconfig.json -t stylish --fix 'src/**/*.ts' 'test/**/*.ts'",
    "prebuild": "rimraf dist",
    "build": "tsc -p tsconfig.json && cp -r src/json dist/json",
    "postbuild": "cp package.json dist && cp LICENSE dist && cp README.md dist && rimraf dist/test",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:ci": "jest --coverage --no-cache",
    "travis-deploy-once": "travis-deploy-once",
    "semantic-release": "cd dist && semantic-release"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "23.3.11",
    "@types/js-yaml": "3.11.4",
    "@types/lodash": "4.14.119",
    "@types/node": "10.12.18",
    "@types/prettier": "1.15.2",
    "@types/rimraf": "2.0.2",
    "jest": "23.6.0",
    "js-yaml": "3.12.1",
    "lodash": "4.17.11",
    "prettier": "1.15.3",
    "rimraf": "2.6.3",
    "semantic-release": "15.13.2",
    "travis-deploy-once": "5.0.11",
    "ts-jest": "23.10.5",
    "ts-node": "7.0.1",
    "tslint": "5.12.0",
    "tslint-config-prettier": "1.17.0",
    "tslint-xo": "0.11.0",
    "typescript": "3.2.2"
  },
  "publishConfig": {
    "access": "public"
  },
  "release": {
    "branch": "master"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
